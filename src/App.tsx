import HomePage from '@/pages/HomePage';
import '@/App.css';

/**
 * The root component of the application.
 *
 * This component serves as the entry point for the user interface, rendering the
 * `HomePage` which contains the main content of the application.
 *
 * @returns The rendered `App` component.
 */
function App() {
  return <HomePage />;
}

export default App;